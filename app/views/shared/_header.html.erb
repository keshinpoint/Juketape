<nav class='navbar navbar-default'>
  <div class='container-fluid'>
    <div class='navbar-header'>
      <%= link_to root_path, class: 'navbar-brand' do %>
        <%= image_tag 'Juketape_logo_v2_smal-whiteborder.png', alt: 'logo' %>
      <% end %>
      <button class='navbar-toggle' type='button' data-toggle='collapse' data-target='.navbar-collapse'> <!-- data -toggle: collapse indicates the button will trigger the collapse behavior -->
        <span class='sr-only'> Menu </span>
        <span class='icon-bar'> </span>
        <span class='icon-bar'> </span>
        <span class='icon-bar'> </span>
      </button>

      <%= render partial: 'shared/search_form_header' %>
    </div>
    <div class = "navbar-collapse collapse">
    <% if signed_in? %>
      <%= render partial: 'shared/logged_in_nav' %>
    <% else %>
      <%= render partial: 'shared/login_form_header' %>
    <% end %>
  </div>
  </div>
</nav>
